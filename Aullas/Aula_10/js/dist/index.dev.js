"use strict";

var nome = 'Allyson'; // String com aspas simples

var nome_1 = 'Allyson'; // String com aspas duplas

var nome_2 = "Allyson "; // String com crases

var num = 10; // number

var num_1 = 10.52; // number tipo flutuante

var nomeAluno; // undefined -> não aponta para local nenhum na memória.

var sobrenomeAluno = null; // Nulo -> não aponta para local nenhum na memória.

var aprovado = false; // Boolean = true, false (lógico).