const nome = 'Allyson'; // String com aspas simples
const nome_1 = 'Allyson'; // String com aspas duplas
const nome_2 = `Allyson `; // String com crases

const num = 10; // number
const num_1 = 10.52 // number tipo flutuante

let nomeAluno; // undefined -> não aponta para local nenhum na memória.
let sobrenomeAluno = null; // Nulo -> não aponta para local nenhum na memória.

const aprovado = false; // Boolean = true, false (lógico).